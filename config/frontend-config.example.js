// LOCAL-DEVELOPMENT EXAMPLE ONLY. Copy values deliberately; do not deploy as-is.
// This public file must never contain passwords, API keys, JWTs, or database URLs.
window.VISIT_LIBYA_CONFIG = Object.freeze({
  apiBaseUrl: "http://127.0.0.1:8001/api/v1",
  apiEnabled: true,
  debug: false,
  requestTimeoutMs: 10000,
  deploymentEnvironment: "local",
  siteBasePath: "",
  defaultLocale: "en",
});
