# Provider-Neutral Deployment Package

This directory contains non-executing contracts, examples, checklists, and manual runbooks. Nothing here provisions infrastructure, deploys software, connects to a database, or authenticates to a cloud. No file contains production secrets. Provider-specific adapters are deliberately deferred until a separately approved provider decision. Isolated staging validation is mandatory before production approval.

